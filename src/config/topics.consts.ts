import * as Core from './LANDING.md';

const { title, topics, extra_text } = Core.frontmatter.primary_topics;

export const TOPICS_LIST = topics;

export const TOPICS_TITLE = title;

export const TOPICS_EXTRA = extra_text;
